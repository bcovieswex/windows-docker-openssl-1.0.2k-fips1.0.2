# windows-docker-openssl-1.0.2k-fips1.0.2
Docker image to help windows machines to use openssl-1.0.2k-fips 

Useful commands:
docker build --progress=plain -t centos7-openssl-102k .

docker compose up -d

docker compose exec ssl bash